import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  headerLeft: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    width: 50,
    height: 30,
  },
})
